import { createTheme } from "@mui/material/styles";

export const mainTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#19528c",
    },
    secondary: {
      main: "#d41b64",
    },
  },
});
